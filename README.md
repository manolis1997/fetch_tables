# Fetch Tables

## Overview
This project exists to fetch the correct catalog, schema, and table names based on the selected environment.

## Supported Environments
- dev
- stg
- prd

## Installation
To run this project, you must install the required dependencies:

```bash```
pip install -r requirements.txt

## Run the project
python .main.py -env

## Test
python -m pytest
